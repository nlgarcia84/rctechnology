---
import {
  Heart,
  Activity,
  Scale,
  Zap,
  Link2,
  BellRing,
  ShieldCheck,
  Watch,
  HeartPulse,
  FlaskConical,
  Droplets,
  ScanLine,
  Building2,
} from 'lucide-react';

interface Props {
  icon?: string;
  title: string;
  description: string;
}

const { icon, title, description } = Astro.props;

const iconMap = {
  Heart,
  Activity,
  Scale,
  Zap,
  Link2,
  BellRing,
  ShieldCheck,
  Watch,
  HeartPulse,
  FlaskConical,
  Droplets,
  ScanLine,
  Building2,
};

const IconComponent =
  icon && icon in iconMap ? iconMap[icon as keyof typeof iconMap] : null;
---

<div class="text-center">
  {
    IconComponent && (
      <IconComponent className="w-6 h-6 text-brand-500 mb-3 ml-auto mr-auto" />
    )
  }

  <span class="text-xl font-bold tracking-tight bg-gradient-to-r from-white to-gray-300 bg-clip-text text-transparent"> {title} </span>
  <p class="mt-2 text-sm text-gray-400 leading-relaxed">
    {description}
  </p>
</div>
